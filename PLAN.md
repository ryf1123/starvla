# StarVLA · 本地闭环 VLA 计划

目标不是刷 benchmark，是**亲手把 VLA 的每一块拆开看懂**：
机器人怎么被控制、数据从哪来、图像和语言怎么变成动作、为什么闭环成功率和训练 loss 是两回事、
哪一块换掉会掉多少分。

机器：M4 10 核 / 16 GB，无 CUDA。MuJoCo CPU + PyTorch MPS。

一句话的系统：**每 1/20 秒，策略看一张前视图、一张腕部图、一句中文指令和自己的状态，
输出末端接下来 8 步的位移和夹爪开合；环境用 IK 把它变成关节指令。**

---

## 0. 环境与世界（✅ 已完成）

- `uv` 3.11 venv + mujoco 3.12 + torch 2.13（MPS 可用）+ mujoco_menagerie。
- `sim/assets.py` 程序化搭场景：Panda + 桌子 + 3 个彩色方块 + 盘子 + 前视/腕部两路相机。
- `sim/ik.py` 阻尼最小二乘 IK：TCP 目标位姿 → 7 个关节角。
- `sim/tabletop_env.py` 20 Hz 控制环、5 维末端动作、成功判据、场景随机化。

验收：`python -m sim.assets` 打印场景规格；渲染出的两路图像里能看清方块和夹爪。

## 1. 专家与数据（✅ 已完成）

- `expert/scripted.py` 7 段状态机（悬停→下降→闭合→抬起→平移→放下→松手），用特权信息。
- `expert/collect.py` 8 进程采集，只留成功轨迹，动作上加高斯噪声扩展状态覆盖。

验收：专家成功率 100%；500 条演示 ≈ 1.6 万步，`videos/expert_demo.mp4`。

**要体验的坑**：噪声为 0 时数据只覆盖一条细线，策略一偏就没见过（复合误差）；
夹爪闭合前不"停几步"会在接触没稳定时就抬手。

## 2. 第一环：行为克隆能不能闭环（✅ 已完成，74%）

**结果**：`bc_v3`，1.33 M 参数，800 条演示，16000 步（40 分钟），
50 局闭环 **74%**（抓错方块 0 / 没抓起来 5 / 没进盘子 8）。
过程见 `notes/02-闭环失败诊断.md`：第一次只有 34%，靠"失败归因 → 闭合时刻的毫米数 →
直接量视觉特征"三步定位到空间 softmax 失灵，另外两个修法是初始姿态抖动和恢复数据。

## 2b. 第一环（原始计划）

`policy/` 四件套：数据集（动作分块）→ 模型（CNN + 空间 softmax + 语言 Transformer + FiLM）→ 训练 → 闭环评测。

- 关键指标是**闭环成功率**，不是 L1。要专门画一张"val L1 下降但成功率不动"的图。
- 时间集成（temporal ensembling）开/关对比。
- 动作分块长度 H = 1 / 4 / 8 / 16 扫一遍。

验收：成功率 > 60%；一段成功视频 + 一段典型失败视频（并排）。

## 3. 第二环：语言到底有没有在起作用（第 2 周）

桌上 3 个不同颜色方块，只有指令能告诉策略抓哪个。三组对照：

| 设置 | 预期 |
|---|---|
| `lang=none` | 只能瞎猜，成功率 ≈ 1/3 |
| `lang=bow` 字符词袋 | 「红→黄」和「黄→红」编码相同，放错盘子 |
| `lang=seq` 有序编码 | 正常 |
| `film=false` | 语言只在最后拼接，定位变差 |

还要做**指令换句式**测试（「拿起红色方块」vs「把红色的方块抓起来」），看它学的是模板还是语义。
产出：一张"同一初始状态、只改指令，机械臂走向不同方块"的同屏对比视频——这是整个项目最直观的一张图。

## 4. 第三环：视觉表征与鲁棒性（第 3 周）

- 从零 CNN vs ImageNet 预训练 ResNet18（冻结/微调）；数据量 50/100/200/500 条的成功率曲线。
- 只用前视 vs 只用腕部 vs 两路（腕部相机在最后 2 cm 决定成败）。
- 分布外测试：干扰物增加到 5 个、方块出现在训练没覆盖的角落、光照和桌面颜色扰动。
- 关键坑：**色彩增强会毁掉颜色条件任务**，要有"加了色相抖动后成功率掉多少"的实测。

## 5. 第四环：动作表示的三条路线（第 4 周）

同一份数据、同一个视觉/语言骨干，只换动作头：

| 路线 | 代表 | 实现 |
|---|---|---|
| 连续回归 + 分块 | ACT | 已有（L1） |
| 离散 token + 自回归 | RT-1 / OpenVLA | 每维 256 bin，交叉熵 |
| 条件扩散 | Diffusion Policy | 10 步 DDIM 去噪 |

比成功率、推理延迟（M4 上每步多少毫秒）、多峰行为（放盘子左边还是右边）的表现。

## 6. 第五环：换成真的 VLM 骨干（第 5–6 周，挑战项）

把手写的 CNN + 字符 Transformer 换成一个真的预训练视觉语言模型（SmolVLM-256M 这一档），
LoRA 微调，动作作为特殊 token 输出。这一步才对得起 "VLA" 三个字母。

要回答的问题：预训练的视觉语言知识能不能带来**没见过的指令措辞/没见过的颜色词**上的泛化？
代价是什么（显存、每步延迟、训练时间）？16 GB 上跑不动就如实记录跑不动在哪。

## 7. 第六环：泛化边界（收尾）

系统地测：新颜色组合 / 新措辞 / 新位置 / 多步指令（「先放红的再放绿的」）/ 更多干扰物。
产出一张失效边界表——哪些能泛化、哪些一碰就碎，以及"数据里缺了什么"的解释。

## 明确不做

真机部署、需要 CUDA 的训练框架、大规模数据集（OXE 全量）、复杂长程任务（叠杯子/开抽屉）。

## 时间线

| 周 | 产出 |
|---|---|
| 0.5 | 环境 + 专家 + 500 条数据（已完成） |
| 1 | BC 闭环成功率 > 60%，成功/失败视频 |
| 2 | 语言消融三组 + 「改指令换目标」对比视频 |
| 3 | 视觉表征与数据量曲线、分布外测试 |
| 4 | 三种动作头对比表 |
| 5–6 | 真 VLM 骨干 LoRA（挑战项） |
| 7 | 泛化边界表 + 总结 |

## 三个提醒

- **闭环成功率是唯一指标**。val loss 好看没有用，每次消融都必须跑 ≥ 50 局闭环。
- **一次只改一个变量**，改完把同屏对比视频录下来，否则事后说不清是哪一项起的作用。
- **每一环一页 notes**，这是这个项目真正的产出。

# StarVLA · 计划（2026-08-24 重排；夜间更新）

目标不是刷 benchmark，是**亲手把 VLA 的每一块拆开看懂**。
机器：M4 10 核 / 16 GB，无 CUDA。MuJoCo CPU + PyTorch MPS。

一句话的系统：**每 1/20 秒，策略看一张前视图、一张腕部图、一句中文指令和自己的状态，
输出末端接下来 8 步的位移和夹爪开合；环境用 IK 把它变成关节指令。**

---

## 已完成

| 环 | 内容 | 结果 |
|---|---|---|
| 0 | 世界：Panda 桌面场景、阻尼最小二乘 IK、20 Hz 控制环、两路相机 | ✅ |
| 1 | 脚本专家 + 演示数据（含 DAgger 式恢复数据） | ✅ 专家 100% |
| 2 | 第一环：行为克隆闭环 | ✅ **87.5%**（40 局），过程见 `notes/02` |
| 3 | 第二环：语言消融五组 | ✅ 见下表 |
| 4 | 泛化边界七项 | ✅ 换措辞 0% 是最大的洞 |
| 5 | 第五环：预训练语言编码器 | ✅ 换措辞 0% → 76% |
| 6 | 失败归因做到位（分类改了三次） | ✅ 病因是抓取对准，见 `notes/12` |
| 7 | 域随机化 | ✅ 是权衡不是白拿，见 `notes/13` |
| 8 | 多步历史观测 | ✅ **第一个统计显著的改进**，91.0% vs 84.0%，p=0.034 |
| 9 | 辅助定位监督 | ✅（负结果）−62 个百分点，见 `notes/15` |
| 10 | 训练种子方差 | ✅ ±2 个百分点，划出"小于 4 个点不能只报单种子"这条线 |
| 11 | 动作表示三条路线 | ✅ 回归 93.8% / 离散 35–46% / 扩散 3.8–5.0%，见 `notes/17` |
| 12 | 造一个真正多峰的任务 | ✅ 同一个解码开关在两个阶段上符号相反，见 `notes/18` |
| 13 | 扩大指令空间到 36 条 | ✅ 93.8% → 47.5%，但"听懂"没退化，见 `notes/20` |
| — | 时间集成扫描、评测噪声量化、文献调研 | ✅ |

**目前最佳配置**：`runs/bc_v5_hist`（`place_v2` 数据 + 最近 3 帧本体历史 + 字符级 CLS 语言
+ FiLM + ensemble_k=1），80 局 **93.8%**，三个训练种子 93.8 / 91.2 / 90.0%。

**语言这一环的结论**：能不能表达顺序是 80% 和 12.5% 的分界；能不能在视觉里生效（FiLM）
是 72.5% 和 5% 的分界。详见 `notes/03`。

**两次被自己的对照实验推翻的因果判断**（`notes/02`）——这是这个项目最值得读的部分。

---

## 正在做（文献驱动，见 `docs/lit-review.md`）

调研给出的"如果只能做三件事"，和我们的实测缺口高度重合：

### A. 把评测做扎实（结构性优势）

PhAIL（2026）调查 13 篇真机 VLA 论文，每条件众数 N=10–20，**没有一篇报告置信区间或配对检验**；
±5 个百分点的 95% 区间需要约 380 次 rollout。**我们在 MuJoCo 里 rollout 几乎免费。**

- [x] 每组报 95% Wilson 区间
- [x] 量化混沌翻转率（5%，总成功率不变）
- [x] `scripts/compare.py`：配对同种子 + **McNemar 精确检验**
- [x] 关键比较跑到 200 局（多步历史：91.0% vs 84.0%，McNemar p=0.034）
- [x] 每个配置训 ≥3 个随机种子（`bc_v5_hist` / `_s1` / `_s2`：93.8 / 91.2 / 90.0%）

### B. 域随机化，尤其是相机位姿

《Decomposing the Generalization Gap》(ICRA 2024)：泛化难度**相机位姿最高**
（真机 91.7% → 45.8%）；《Data Scaling Laws》(ICLR 2025)：泛化对**环境数**呈幂律，
对每环境演示数很快饱和（32 环境 × 50 条 → 新环境新物体 90%）。

- [x] `sim/randomize.py`：相机位姿 / 光照 / 桌面颜色 / 明暗（**不动色相**，色相是标签）
- [x] 泛化测试加 `camera` 和 `light` 两项
- [x] `bc_v5_dr`：分布内 83→63（−20），相机偏移 30→40，光照 27→50（+23）

### C. 多步历史观测

RoboVLMs（600+ 组受控实验，2026 Nature MI）：**多步历史观测 + 连续动作 + 独立 policy head**
是最好的组合，且与模型规模无关。

- [x] `model.state_history=k`：最近 k 帧的 (本体状态, 上一步动作)
- [x] `bc_v5_hist`：k=3 vs k=1，**84.0% → 91.0%**（200 局配对，p=0.034）——目前最佳配置

### D. 放置精度（针对当前最大的失败桶）

87.5% 那版的失败构成是「没进盘子 4 / 没抓起来 1」。专家原本在离盘面 3.5 cm 处松手，
方块自由落体；改成 1.2 cm 并要求松手时水平仍对准。专家落点中位数离盘心 5.6 mm。

- [x] 改专家 + 重采数据 `place_v2`
- [x] `bc_v5_place`：**预注册的预测被证实**，McNemar p=1.000，改松手高度毫无作用；
      真正的病因是抓取对准（`notes/12`）

---

## 之后

### 扩大指令空间 —— ✅ 第一轮已完成（`notes/20`），但结论指向新问题

`bc_rel`（36 条指令的数据）只有 37.5%，但它用的是**旧配置**（`ss_raw=true` /
`ensemble_k=4` / 无历史），"任务变难"和"配置缺陷"混在一起了。
结果：`rel_cls_hist` 和 `rel_ppool_hist` **都是 47.5%**（McNemar p=1.000）。
拆开看：带位置词 27.9%、不带位置词 70.3%，而**抓错方块只有 3 局**——
掉的不是"听懂"，是手上的精度。四个机制假设否掉三个、第四个量级不够。
剩下唯一的候选是**数据密度**（900 条摊到 36 条指令 = 每条 25 局），
`rel_cls_big`（2700 条，每条 75 局）在队列里。

顺带回答了 `notes/10` 的悬念：**36 条还不够大**，预训练语言编码器仍然没有分布内收益。


训练指令原本只有 **12 条**（4 色 × 3 个不同色的盘子），"语言理解"实质是 12 选 1 的分类。
已迈第一步：同色方块 + 「左边的/右边的」→ **36 条**（`notes/09`）。
再往下：物体大小、材质、更多空间关系（"最靠近盘子的那个"）、多步指令、否定式。
**指令空间必须大到模型没法靠背下来解决**，"语言"这一维才有意义。

### 动作表示三条路线 —— ✅ 已完成（`notes/17`、`notes/18`）

回归 93.8% / 离散 argmax 35.0% / 离散期望解码 46.2% / 扩散 3.8–5.0%。
预注册的预测（"三者差别不大"）被推翻。文献的建议是：
**没有 LM backbone 就别把动作离散成 token**（量化误差 + 解码开销，唯一的理由是复用 token 接口）。
所以这一组的优先级下调，主要价值是验证"多峰表示能不能救 `lang_none` 那种平均动作失效"。

### 明确不做

- 换 DINOv2/SigLIP 大编码器——比整个策略大两三个数量级，且文献显示跨环境泛化不可靠
- 在 LIBERO 上刷分——已"基本被解决"（95–98%）且高分不迁移
- 真机部署、需要 CUDA 的框架

---

## 三条纪律（吃过亏总结的）

1. **只信闭环成功率**，每组 ≥ 40 局（关键比较 200 局），必带 95% 区间。
2. **一次只改一个变量**，改完立刻做对照。代理指标只能提假设，不能当结论。
3. **失败必须分类**——同样的成功率可以来自完全不同的病因。
